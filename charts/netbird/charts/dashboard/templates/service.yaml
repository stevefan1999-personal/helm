{{- include "netbird-dashboard-lib.service" (dict "root" . "values" .Values "fullname" (include "dashboard.fullname" .) "labels" (include "dashboard.labels" .) "selectorLabels" (include "dashboard.selectorLabels" .) "portName" "http") }}
