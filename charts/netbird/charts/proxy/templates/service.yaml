{{- include "netbird-proxy-lib.service" (dict "root" . "values" .Values "fullname" (include "proxy.fullname" .) "labels" (include "proxy.labels" .) "selectorLabels" (include "proxy.selectorLabels" .) "portNames" (dict "https" "https" "health" "health")) }}
