{{- include "netbird-proxy-lib.crowdsecSecret" (dict "root" . "values" .Values "fullname" (include "proxy.fullname" .) "labels" (include "proxy.labels" .) "crowdsecSecretName" (printf "%s-crowdsec-secrets" (include "proxy.fullname" .)) ) }}
