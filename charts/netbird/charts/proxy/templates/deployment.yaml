{{- include "netbird-proxy-lib.deployment" (dict "root" . "values" .Values "fullname" (include "proxy.fullname" .) "labels" (include "proxy.labels" .) "selectorLabels" (include "proxy.selectorLabels" .) "managementAddress" (include "proxy.managementAddress" .) "proxySecretName" (printf "%s-proxy-secrets" (include "proxy.fullname" .)) "crowdsecSecretName" (printf "%s-crowdsec-secrets" (include "proxy.fullname" .)) "pvcName" (printf "%s-certs" (include "proxy.fullname" .)) "portNames" (dict "https" "https" "health" "health" "acme" "acme-http") "volumeNames" (dict "certs" "certs" "tlsCert" "tls-cert")) }}
