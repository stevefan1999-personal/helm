{{- $secret := dict "enabled" (.Values.secret.enabled | default true) "name" (printf "%s-secrets" (include "dashboard.fullname" .)) -}}
{{- include "netbird-dashboard-lib.deployment" (dict "root" . "values" .Values "fullname" (include "dashboard.fullname" .) "labels" (include "dashboard.labels" .) "selectorLabels" (include "dashboard.selectorLabels" .) "secret" $secret "portName" "http") }}
