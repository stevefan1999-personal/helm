{{- include "netbird-proxy-lib.secret" (dict "root" . "values" .Values "fullname" (include "proxy.fullname" .) "labels" (include "proxy.labels" .) "proxySecretName" (printf "%s-proxy-secrets" (include "proxy.fullname" .)) ) }}
