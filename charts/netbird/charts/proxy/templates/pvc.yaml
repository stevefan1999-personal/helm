{{- include "netbird-proxy-lib.pvc" (dict "root" . "values" .Values "fullname" (include "proxy.fullname" .) "labels" (include "proxy.labels" .) "pvcName" (printf "%s-certs" (include "proxy.fullname" .)) ) }}
